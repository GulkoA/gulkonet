---
layout: '../../layouts/Based.astro'
title: 'Projects'
description: 'A listing of all blog posts by Alex Gulko. I write about research, design, and engineering.'
---
import { getCollection } from "astro:content";
import Based from "../../layouts/Based.astro";
import ProjectCard from "../../components/ProjectCard.astro";
export const pages = (await getCollection('projects')).sort((a, b) => {
  return new Date(b.data.created).getTime() - new Date(a.data.created).getTime();
});

# Projects

I worked on many projects, some of which are listed below.

{pages.map((page) => (
  <ProjectCard
      title={page.data.title}
      description={page.data.description}
      image={page.data.image}
      color={page.data.color}
      secondaryColor={page.data.secondaryColor}
      tokens={page.data.tokens}
      href={"./" + page.slug}
      tags={page.data.tags}
      font={page.data.font}
    />
))}
